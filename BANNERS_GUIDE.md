# دليل إدارة البانرات الإعلانية

## نظرة عامة
تم تطوير نظام البانرات ليكون ديناميكياً وقابلاً للتحكم بالكامل من Firebase Firestore. يدعم النظام عرض حتى **5 بانرات إعلانية** في الصفحة الرئيسية مع إمكانية الربط بروابط خارجية.

---

## إعداد قاعدة البيانات

### 1. إنشاء Collection في Firestore

قم بإنشاء Collection جديدة باسم: **`banners`**

### 2. هيكل المستند (Document)

كل بانر يجب أن يحتوي على الحقول التالية:

| الحقل | النوع | الوصف | مطلوب |
|------|------|-------|-------|
| `imageUrl` | String | رابط الصورة (يفضل رفعها على Firebase Storage أو استخدام روابط خارجية) | ✅ نعم |
| `linkUrl` | String | الرابط الذي سيتم فتحه عند الضغط على البانر (Instagram, موقع إلكتروني، إلخ) | ❌ اختياري |
| `order` | Number | ترتيب ظهور البانر (من 1 إلى 5) - كلما كان الرقم أصغر كلما ظهر أولاً | ✅ نعم |
| `isActive` | Boolean | تفعيل/إلغاء تفعيل البانر (`true` لعرضه، `false` لإخفائه) | ✅ نعم |

---

## مثال عملي لإضافة بانر

### الطريقة الأولى: من Firestore Console

1. افتح Firebase Console
2. اذهب إلى **Firestore Database**
3. اضغط على **Start collection**
4. أدخل اسم الـ Collection: `banners`
5. اضغط **Next**
6. أدخل Document ID (أو اترك Firebase يُنشئ معرفاً تلقائياً)
7. أضف الحقول التالية:

```
Field           Type      Value
-----           ----      -----
imageUrl        string    https://example.com/banner1.jpg
linkUrl         string    https://www.instagram.com/yourpage/
order           number    1
isActive        boolean   true
```

8. اضغط **Save**

---

### الطريقة الثانية: باستخدام Firebase Storage

#### 1. رفع الصورة:
- اذهب إلى **Storage** في Firebase Console
- ارفع صورة البانر (الأبعاد المُنصح بها: **1200×300 بكسل**)
- انسخ رابط الصورة (Download URL)

#### 2. إضافة البانر:
- ارجع إلى Firestore
- أضف مستند جديد في `banners` مع رابط الصورة

---

## أمثلة لبانرات جاهزة

### بانر 1: ترحيب بالمستخدمين
```json
{
  "imageUrl": "https://your-firebase-storage.com/banners/welcome.png",
  "linkUrl": "",
  "order": 1,
  "isActive": true
}
```

### بانر 2: إعلان على Instagram
```json
{
  "imageUrl": "https://your-firebase-storage.com/banners/instagram_ad.png",
  "linkUrl": "https://www.instagram.com/al_askar_company/",
  "order": 2,
  "isActive": true
}
```

### بانر 3: عرض خاص
```json
{
  "imageUrl": "https://your-firebase-storage.com/banners/special_offer.png",
  "linkUrl": "https://your-website.com/offers",
  "order": 3,
  "isActive": true
}
```

### بانر 4: صفحة التواصل
```json
{
  "imageUrl": "https://your-firebase-storage.com/banners/contact_us.png",
  "linkUrl": "https://wa.me/9647xxxxxxxxx",
  "order": 4,
  "isActive": true
}
```

### بانر 5: إعلان تجاري
```json
{
  "imageUrl": "https://your-firebase-storage.com/banners/commercial.png",
  "linkUrl": "https://example.com/product",
  "order": 5,
  "isActive": true
}
```

---

## تعديل/إخفاء البانرات

### إخفاء بانر مؤقتاً:
غيّر قيمة `isActive` من `true` إلى `false`

### تغيير ترتيب البانرات:
عدّل قيمة `order` للبانر (1 = الأول، 5 = الأخير)

### تحديث الصورة:
غيّر قيمة `imageUrl` بالرابط الجديد

### تحديث الرابط:
غيّر قيمة `linkUrl` بالرابط الجديد

---

## ملاحظات مهمة

✅ **يدعم النظام حتى 5 بانرات فقط** - إذا أضفت أكثر، سيظهر أول 5 حسب الترتيب

✅ **تحديث تلقائي** - عند تعديل أي بانر في Firestore، لن تحتاج لإعادة بناء التطبيق. فقط أغلق التطبيق وأعد فتحه

✅ **التدوير التلقائي** - البانرات تتغير تلقائياً كل 5 ثوانٍ

✅ **الضغط على البانر** - إذا كان `linkUrl` موجوداً، سيفتح الرابط في متصفح خارجي

❌ **البانرات غير النشطة** - البانرات التي `isActive = false` لن تظهر في التطبيق

---

## الأبعاد المُوصى بها للصور

| المقاس | الأبعاد (بكسل) | الوصف |
|--------|----------------|-------|
| مثالي | **1200 × 300** | أفضل نسبة لعرض البانرات |
| الحد الأدنى | 800 × 200 | جودة مقبولة |
| الحد الأقصى | 1920 × 480 | لن تحتاج أكثر من ذلك |

**ملاحظة**: احرص على أن تكون الصور بجودة عالية ولكن بحجم ملف صغير (يُفضَّل أقل من 500 كيلوبايت)

---

## استكشاف الأخطاء

### البانرات لا تظهر:
1. تأكد من أن `isActive = true`
2. تأكد من وجود حقل `order` وهو رقم صحيح
3. تأكد من صحة رابط الصورة في `imageUrl`
4. تحقق من أن Collection باسم `banners` بالضبط

### البانر يظهر لكن بدون صورة:
- تحقق من صحة الرابط في `imageUrl`
- إذا كانت الصورة في Firebase Storage، تأكد من أن Firebase Storage Rules تسمح بالقراءة:
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

### الضغط على البانر لا يفتح الرابط:
- تحقق من أن `linkUrl` يحتوي على رابط صحيح يبدأ بـ `https://` أو `http://`

---

## مثال كامل لإضافة 5 بانرات

قم بإضافة هذه المستندات في Collection `banners`:

**Document 1** (ID: banner1)
```json
{
  "imageUrl": "https://firebasestorage.googleapis.com/.../banner_welcome.png",
  "linkUrl": "",
  "order": 1,
  "isActive": true
}
```

**Document 2** (ID: banner2)
```json
{
  "imageUrl": "https://firebasestorage.googleapis.com/.../banner_instagram.png",
  "linkUrl": "https://www.instagram.com/ahmad.alrawiii/",
  "order": 2,
  "isActive": true
}
```

**Document 3** (ID: banner3)
```json
{
  "imageUrl": "https://firebasestorage.googleapis.com/.../banner_ad.png",
  "linkUrl": "https://www.instagram.com/al_askar_company/",
  "order": 3,
  "isActive": true
}
```

**Document 4** (ID: banner4)
```json
{
  "imageUrl": "https://firebasestorage.googleapis.com/.../banner_contact.png",
  "linkUrl": "https://wa.me/9647xxxxxxxxx",
  "order": 4,
  "isActive": true
}
```

**Document 5** (ID: banner5)
```json
{
  "imageUrl": "https://firebasestorage.googleapis.com/.../banner_promo.png",
  "linkUrl": "https://example.com/promo",
  "order": 5,
  "isActive": true
}
```

---

## Firestore Rules المطلوبة

للسماح بقراءة البانرات من التطبيق، تأكد من إضافة هذه القاعدة في Firestore Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بقراءة البانرات لجميع المستخدمين
    match /banners/{bannerId} {
      allow read: if true;
      allow write: if false; // منع التعديل من التطبيق
    }
  }
}
```

---

## خلاصة

✔️ **5 بانرات كحد أقصى**  
✔️ **صور من روابط خارجية أو Firebase Storage**  
✔️ **روابط اختيارية لفتح صفحات خارجية**  
✔️ **ترتيب قابل للتخصيص**  
✔️ **تفعيل/إلغاء تفعيل سهل**  
✔️ **تحديث بدون إعادة بناء التطبيق**

🎉 **الآن يمكنك إدارة البانرات بسهولة من Firebase Console!**

